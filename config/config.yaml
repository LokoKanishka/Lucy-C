# Lucy-C configuration

# ASR
asr:
  model: "Systran/faster-whisper-small"
  device: "cpu"           # fallback-safe (CUDA runtime libs missing on this system)
  compute_type: "int8"    # good speed/compat on CPU
  language: "es"
  task: "transcribe"
  force_language: true

# LLM (Ollama)
ollama:
  host: "http://127.0.0.1:11434"
  model: "gpt-oss:20b"

# TTS (Mimic3 CLI)
tts:
  voice: "es_ES/m-ailabs_low#karen_savage"

# Audio
audio:
  sample_rate: 16000
